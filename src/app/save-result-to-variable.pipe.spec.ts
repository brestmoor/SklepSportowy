import { SaveResultToVariablePipe } from './save-result-to-variable.pipe';

describe('SaveResultToVariablePipe', () => {
  it('create an instance', () => {
    const pipe = new SaveResultToVariablePipe();
    expect(pipe).toBeTruthy();
  });
});
