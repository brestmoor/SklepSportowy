import { StringContainsPipe } from './string-contains.pipe';

describe('StringContainsPipe', () => {
  it('create an instance', () => {
    const pipe = new StringContainsPipe();
    expect(pipe).toBeTruthy();
  });
});
